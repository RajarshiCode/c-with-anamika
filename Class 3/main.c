#include <stdio.h>
#include <math.h>
int main()
{

    /*
        boilerplate code in c

        decision control statements, ternary operator, leap to learn
        switch case, default, need and fall through
        if else, if ladder, else if
        scope of a variable

        if, if else, if else if else, nested if, if else ladder, switch case, ternary        

    */

    int x, y;
    x = abs(5);
    printf("%d\n", x);
    y = abs(-10);
    printf("%d\n", y);
    return 0;
}